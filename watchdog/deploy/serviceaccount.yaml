apiVersion: v1
kind: ServiceAccount
metadata:
  name: watchdog
  namespace: watchdog
  labels:
    app.kubernetes.io/name: watchdog
    app.kubernetes.io/part-of: grip-watchdog
  annotations:
    # snapshot-cli uses --environment flag (internal auth) — no GCS SA needed.
    app.kubernetes.io/managed-by: "watchdog"
automountServiceAccountToken: false
