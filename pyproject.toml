[project]
name = "grip-cluster-watchdog"
version = "0.1.0"
description = "Proactive monitoring agent for Grip Security CAST AI clusters"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "anthropic>=0.40.0",
]

[project.scripts]
watchdog = "watchdog.main:main"
